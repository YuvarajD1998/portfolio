/**
 * Page templates — the layouts pages extend (Sprint 03 §08).
 *
 * A future page picks a template and fills its slots rather than inventing a
 * layout (S03 §08 RULE). Every template opens with a shared PageHeader (the one
 * H1) and composes only Sprint 02 primitives.
 */
export { PageHeader } from './PageHeader';
export { StandardPage } from './StandardPage';
export { ContentPage } from './ContentPage';
export { CaseStudyPage } from './CaseStudyPage';
export { DocumentationPage } from './DocumentationPage';
export { ContactPage } from './ContactPage';
